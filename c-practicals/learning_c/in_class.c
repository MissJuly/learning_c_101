#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 Always success
 */
int main(void)
{
	int c = 10;
	
	while (c > 10)
	{
		puts("I must not write code in class");
		c = c - 1;
	}
	return (0);
}
